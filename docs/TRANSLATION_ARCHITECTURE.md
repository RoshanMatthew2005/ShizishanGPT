# 🌍 Translation Feature - System Architecture

## Component Overview

```
┌─────────────────────────────────────────────────────────────────┐
│                      FRONTEND (React)                            │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │              AgriChatbot Component                        │  │
│  │                                                            │  │
│  │  State Variables:                                         │  │
│  │  • selectedLanguage: "hi" | "te" | "ta" | etc.           │  │
│  │  • autoTranslateInput: boolean                           │  │
│  │  • autoTranslateOutput: boolean                          │  │
│  │  • isTranslating: boolean                                │  │
│  │                                                            │  │
│  │  Components:                                              │  │
│  │  ┌────────────┐  ┌──────────────┐  ┌─────────────┐      │  │
│  │  │ Settings   │  │ Language     │  │ Translation │      │  │
│  │  │ Modal      │  │ Selector     │  │ Toggles     │      │  │
│  │  └────────────┘  └──────────────┘  └─────────────┘      │  │
│  │                                                            │  │
│  │  Visual Indicators:                                       │  │
│  │  • Language badge in header                              │  │
│  │  • Translation icons on messages                         │  │
│  │  • "Translating..." status                               │  │
│  └──────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
                              │
                              │ HTTP POST
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                   MIDDLEWARE (Node.js/Express)                   │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │              Translation Router                           │  │
│  │                                                            │  │
│  │  Route: POST /translate                                   │  │
│  │                                                            │  │
│  │  Request:                                                 │  │
│  │  {                                                        │  │
│  │    text: string,                                         │  │
│  │    source_lang: string,                                  │  │
│  │    target_lang: string                                   │  │
│  │  }                                                        │  │
│  │                                                            │  │
│  │  Middleware Chain:                                        │  │
│  │  validateInput → translateController → apiService        │  │
│  └──────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
                              │
                              │ HTTP POST
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                     BACKEND (FastAPI)                            │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │             Translation Service                           │  │
│  │                                                            │  │
│  │  router_general.py:                                       │  │
│  │  @router.post("/translate")                              │  │
│  │  async def translate_text()                              │  │
│  │                                                            │  │
│  │  translate_service.py:                                    │  │
│  │  • Initialize Google Translator                          │  │
│  │  • Perform translation                                   │  │
│  │  • Detect source language                                │  │
│  │  • Return translated text                                │  │
│  │                                                            │  │
│  │  Response:                                                │  │
│  │  {                                                        │  │
│  │    success: true,                                        │  │
│  │    data: {                                               │  │
│  │      translated_text: string,                           │  │
│  │      detected_language: string,                         │  │
│  │      execution_time: float                              │  │
│  │    }                                                     │  │
│  │  }                                                        │  │
│  └──────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
                              │
                              │ API Call
                              ▼
                    ┌─────────────────────┐
                    │  Google Translate   │
                    │       API           │
                    └─────────────────────┘
```

## Data Flow Diagrams

### 1. Input Translation Flow (User → English)

```
┌──────────────┐
│ User types   │  "मेरी फसल में कीड़े लग गए हैं"
│ in Hindi     │
└──────┬───────┘
       │
       ▼
┌──────────────────────────┐
│ Check autoTranslateInput │  = true
└──────┬───────────────────┘
       │
       ▼
┌──────────────────────┐
│ Show "Translating..."│
│ indicator            │
└──────┬───────────────┘
       │
       ▼
┌──────────────────────────────┐
│ api.translateText(           │
│   text: "मेरी फसल...",      │
│   source: "hi",              │
│   target: "en"               │
│ )                            │
└──────┬───────────────────────┘
       │
       ▼
┌──────────────────────────────┐
│ Middleware forwards to       │
│ FastAPI backend              │
└──────┬───────────────────────┘
       │
       ▼
┌──────────────────────────────┐
│ Google Translate API         │
│ translates text              │
└──────┬───────────────────────┘
       │
       ▼
┌──────────────────────────────┐
│ Receive translation:         │
│ "My crop has been infested   │
│  with insects"               │
└──────┬───────────────────────┘
       │
       ▼
┌──────────────────────────────┐
│ Use English text for         │
│ AI Agent query               │
└──────┬───────────────────────┘
       │
       ▼
┌──────────────────────────────┐
│ Add message with             │
│ translatedText indicator     │
└──────────────────────────────┘
```

### 2. Output Translation Flow (English → User's Language)

```
┌──────────────┐
│ AI generates │  "To control pests, use organic methods..."
│ response in  │
│ English      │
└──────┬───────┘
       │
       ▼
┌───────────────────────────┐
│ Check autoTranslateOutput │  = true
└──────┬────────────────────┘
       │
       ▼
┌──────────────────────┐
│ Show "Translating..."│
│ indicator            │
└──────┬───────────────┘
       │
       ▼
┌──────────────────────────────┐
│ api.translateText(           │
│   text: "To control...",     │
│   source: "en",              │
│   target: "hi"               │
│ )                            │
└──────┬───────────────────────┘
       │
       ▼
┌──────────────────────────────┐
│ Google Translate API         │
│ translates response          │
└──────┬───────────────────────┘
       │
       ▼
┌──────────────────────────────┐
│ Receive translation:         │
│ "कीट नियंत्रण के लिए,       │
│  जैविक तरीकों का उपयोग..."  │
└──────┬───────────────────────┘
       │
       ▼
┌──────────────────────────────┐
│ Display translated text      │
│ to user                      │
└──────┬───────────────────────┘
       │
       ▼
┌──────────────────────────────┐
│ Add originalText field       │
│ Show translation indicator   │
└──────────────────────────────┘
```

### 3. Complete Conversation Flow

```
   USER INPUT (Hindi)
         │
         ▼
   ┌─────────────┐
   │ Translation │ → English
   │   (Input)   │
   └──────┬──────┘
          │
          ▼
   ┌─────────────┐
   │  AI Agent   │ → Process query
   │  Processing │   (English)
   └──────┬──────┘
          │
          ▼
   ┌─────────────┐
   │  Response   │ → English response
   │  Generated  │
   └──────┬──────┘
          │
          ▼
   ┌─────────────┐
   │ Translation │ → Hindi
   │  (Output)   │
   └──────┬──────┘
          │
          ▼
   USER RECEIVES (Hindi)
```

## State Management

### Translation State Variables

```javascript
// Language preference
const [selectedLanguage, setSelectedLanguage] = useState("en");
// Values: "en", "hi", "te", "ta", "kn", "ml", "mr", "bn", "gu", "pa"

// Translation toggles
const [autoTranslateInput, setAutoTranslateInput] = useState(false);
const [autoTranslateOutput, setAutoTranslateOutput] = useState(false);

// Translation status
const [isTranslating, setIsTranslating] = useState(false);
```

### Message Structure

```javascript
// User message with translation
{
  id: 123,
  type: "user",
  text: "मेरी फसल में कीड़े लग गए हैं",          // Original
  translatedText: "My crop has insects",         // Translated
  timestamp: Date,
  files: []
}

// Bot message with translation
{
  id: 124,
  type: "bot",
  text: "कीट नियंत्रण के लिए...",               // Translated
  originalText: "To control pests...",            // Original
  timestamp: Date,
  data: {}
}
```

## UI Components

### Settings Modal
```
┌─────────────────────────────┐
│ ⚙️  Settings               │
├─────────────────────────────┤
│ Query Mode:                 │
│ [Agent ▼]                   │
│                             │
│ 🌐 Preferred Language:      │
│ [हिन्दी (Hindi) ▼]         │
│                             │
│ ┌─────────────────────────┐ │
│ │ Auto-translate Input    │ │
│ │ Translate your messages │ │
│ │ to English     [ON] ●─  │ │
│ └─────────────────────────┘ │
│                             │
│ ┌─────────────────────────┐ │
│ │ Auto-translate Output   │ │
│ │ Translate bot responses │ │
│ │ to your language [ON] ●─│ │
│ └─────────────────────────┘ │
│                             │
│ [Save Changes]              │
└─────────────────────────────┘
```

### Language Indicator (Header)
```
┌───────────────────────────────────┐
│ [☰] 🌱 ShizishanGPT     [🌐 HI ●]│
│     Your AI farming companion     │
└───────────────────────────────────┘
         ↑                     ↑
    Menu Icon          Language Badge
                       (pulsing when active)
```

### Message with Translation Indicator
```
┌─────────────────────────────────────┐
│ 🌱 ShizishanGPT                     │
│                                     │
│ कीट नियंत्रण के लिए जैविक तरीकों  │
│ का उपयोग करें...                   │
│                                     │
│ ─────────────────────────────────   │
│ 🌐 Auto-translated                  │
│                                     │
│ 9:30 AM                             │
└─────────────────────────────────────┘
```

## Error Handling

### Translation Failures
```
1. Network Error
   ↓
   Log error to console
   ↓
   Continue with original text
   ↓
   Show warning icon

2. Invalid Language Code
   ↓
   Validate before API call
   ↓
   Use fallback (English)

3. API Timeout
   ↓
   Set timeout to 10s
   ↓
   Retry once
   ↓
   Fall back to original text
```

## Performance Optimization

### Current Implementation
- No caching
- Sequential translation (input then query then output)
- Average time: 0.6-1.2 seconds per translation

### Future Optimizations
```
1. Translation Cache
   Common phrases → Local storage
   Reduces API calls by ~40%

2. Batch Translation
   Multiple messages → Single API call
   Reduces latency by ~30%

3. Predictive Translation
   Pre-translate common responses
   Near-instant display
```

---

**Last Updated**: December 8, 2025  
**Diagram Version**: 1.0
